---
paths:
  - '**'
---

# General

## Code and variables in English
All code must be written in English: variable names, method names, class names, file names, comments, database column names, route names, event names, job names, config keys, etc. French is reserved for user-facing content only (translation files, blade views via translation keys). Never mix French into code identifiers.
